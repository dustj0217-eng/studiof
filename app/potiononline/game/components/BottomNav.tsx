'use client';
import { useGame } from '../lib/gameStore';
export default function BottomNav(){const {state,dispatch}=useGame();const tabs=[{id:'home' as const,label:'홈',icon:'⌂'},{id:'orders' as const,label:'주문',icon:'▤',badge:state.orders.length},{id:'products' as const,label:'상품',icon:'◇'},{id:'customers' as const,label:'고객',icon:'♡'},{id:'my' as const,label:'MY',icon:'○'}];return <nav className="nav">{tabs.map(t=><button key={t.id} className={state.tab===t.id?'on':''} onClick={()=>dispatch({type:'SET_TAB',tab:t.id})}><span>{t.icon}</span><small>{t.label}</small>{t.badge?<i>{t.badge}</i>:null}</button>)}<style jsx>{`.nav{position:sticky;bottom:0;z-index:70;display:grid;grid-template-columns:repeat(5,1fr);background:rgba(255,255,255,.98);border-top:1px solid #ead7e5;padding:6px 5px calc(7px + env(safe-area-inset-bottom));box-shadow:0 -8px 20px rgba(91,51,83,.05)}button{border:0;background:none;color:#aa96a7;display:flex;flex-direction:column;align-items:center;gap:2px;position:relative;padding:4px 0}button>span{font-size:20px;line-height:20px}button small{font-size:9px;font-weight:700}.on{color:#ff3f91}.on>span{filter:drop-shadow(2px 2px 0 #ffd0e3)}i{position:absolute;top:-2px;left:calc(50% + 9px);background:#7655d7;color:white;font-size:7px;font-style:normal;min-width:14px;height:14px;padding:0 3px;border-radius:8px;display:grid;place-items:center;border:2px solid white}`}</style></nav>}
